{
  "name": "@vidpulse/config",
  "version": "1.0.0",
  "description": "Typed, schema-validated (zod) YAML configuration with per-environment sections and file migrations",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  },
  "dependencies": {
    "yaml": "^2.8.3",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^6.0.3",
    "vitest": "^2.1.8"
  }
}
