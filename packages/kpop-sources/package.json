{
  "name": "@vidpulse/kpop-sources",
  "version": "1.0.0",
  "description": "Parse external K-pop data sources (Wikidata) into a VidPulse media-library snapshot",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^2.1.8"
  }
}
