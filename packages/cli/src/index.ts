/**
 * @vidpulse/cli — command-line tools for VidPulse.
 *
 * The runnable entry points live in `src/cli/*` and are exposed as `bin`s (see package.json) or via
 * the `tsx`-based npm scripts. This barrel exists only to give the package a valid `main`; the CLIs
 * are side-effecting scripts, not a library API.
 */
export {};
