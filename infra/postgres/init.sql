-- Create extensions
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- Create additional indexes (Prisma will create tables)
-- This file can be used for any custom SQL that needs to run on database initialization